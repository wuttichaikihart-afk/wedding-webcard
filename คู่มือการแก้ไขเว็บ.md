# 📖 คู่มือการแก้ไขและปรับแต่งเว็บไซต์การ์ดแต่งงาน (Web Wedding Card)

คู่มือฉบับนี้จัดทำขึ้นเพื่อให้คุณสามารถเข้ามาแก้ไขข้อมูลต่างๆ บนเว็บไซต์ได้ด้วยตัวเองอย่างง่ายดาย โดยจะแบ่งออกเป็นหมวดหมู่ต่างๆ ตามส่วนที่ต้องการแก้ไข

---

## 🎨 1. วิธีการเปลี่ยนสีหลักของเว็บไซต์ (Theme Colors)
หากต้องการเปลี่ยนสีรวมๆ ของเว็บไซต์ เช่น สีของปุ่ม, สีพื้นหลัง, สีตัวหนังสือหลัก
ให้ไปแก้ไขที่ไฟล์: 📁 `src` > **`index.css`**

เมื่อเปิดไฟล์ขึ้นมา ที่บรรทัดแรกสุดในส่วน `:root { ... }` จะพบตัวแปรสีต่างๆ:
- `--primary`: สีหลักของเว็บ (ปัจจุบันคือสีชมพูตุ่น #D28B93) เช่น สีปุ่ม, สีวงกลมครั่ง, สีเส้นเน้น
- `--primary-light`: สีโทนอ่อนของเว็บ
- `--primary-dark`: สีโทนเข้มสำหรับขอบ หรือส่วนที่ต้องการเน้น
- `--secondary` และ `--bg-color`: สีพื้นหลังของแต่ละ Section
- `--text-main` และ `--text-light`: สีของตัวหนังสือ

**วิธีแก้:** เปลี่ยนรหัสสีตัวเลข 6 ตัว (เช่น `#D28B93`) เป็นรหัสสีที่ต้องการได้เลย

---

## 🖼️ 2. วิธีการเปลี่ยนรูปภาพ

รูปภาพทั้งหมดของเว็บไซต์จะถูกเก็บไว้ที่: 📁 `src` > **`assets`**

- **รูปภาพหน้าปก (Hero Image):** 
  ให้นำรูปปกใหม่ไปวางในโฟลเดอร์ `assets` จากนั้นไปที่ไฟล์ `src/components/HeroSection.jsx` 
  บรรทัดที่ 12: เปลี่ยนชื่อไฟล์ที่ `import heroBg from '../assets/ชื่อไฟล์ของคุณ.jpg';`

- **รูปแกลเลอรี (Our Memories):** 
  ง่ายมาก! เพียงแค่นำรูปของคุณไปวางในโฟลเดอร์ `assets` แล้วตั้งชื่อให้ขึ้นต้นด้วยคำว่า `pic` ตามด้วยตัวเลข เช่น `pic1.jpg`, `pic2.jpg`, `pic21.jpg` เป็นต้น
  *(ระบบจะดึงรูปที่ชื่อขึ้นต้นด้วย pic มาแสดงผลอัตโนมัติ โดยไม่ต้องเขียนโค้ดเพิ่ม)*

- **รูป QR Code โอนเงิน:**
  นำรูป QR Code ใหม่ไปวางในโฟลเดอร์ `assets` จากนั้นไปที่ไฟล์ `src/components/GiftRegistry.jsx` 
  บรรทัดที่ 12: เปลี่ยนชื่อไฟล์ที่ `import qrCodeImg from '../assets/ชื่อไฟล์QRของคุณ.JPG';`

---

## 🎵 3. วิธีเปลี่ยนเพลงพื้นหลัง (ม่านหน้าเว็บ)
เมื่อแขกคลิกเปิดม่าน "ครั่ง" จะมีเพลงดังขึ้น หากต้องการเปลี่ยนเพลงให้ทำดังนี้:
1. เตรียมไฟล์เพลง นามสกุล `.mp3`
2. เปลี่ยนชื่อไฟล์นั้นเป็น **`bgmusic.mp3`** (ตัวพิมพ์เล็กทั้งหมด ไม่มีเว้นวรรค)
3. นำไฟล์นี้ไปวางทับไฟล์เดิมที่โฟลเดอร์: 📁 **`public`** (โฟลเดอร์นอกสุด ไม่ใช่ใน src)

---

## 📝 4. วิธีแก้ไขข้อความในแต่ละส่วนของหน้าเว็บ
ข้อมูลในหน้าเว็บจะถูกแยกส่วนกันอยู่ในโฟลเดอร์ 📁 `src/components` สามารถเลือกแก้ได้ตามไฟล์ ดังนี้:

### 4.1 ชื่อบ่าวสาวตัวใหญ่ หน้าปก
- ไปที่: `src/components/HeroSection.jsx`
- เลื่อนไปแก้บรรทัดที่เขียนว่า `Toey & Kratai`

### 4.2 วันที่จัดงาน และ เวลานับถอยหลัง
- ไปที่: `src/components/Countdown.jsx`
- การตั้งค่านับถอยหลัง: แก้ที่บรรทัด `const targetDate = new Date('2026-12-19T07:09:00');`
- แก้วันที่ที่แสดงผลตัวอักษร: เลื่อนหาคำว่า `19 ธันวาคม 2026`

### 4.3 กำหนดการพิธีการ (Schedule of Events)
- ไปที่: `src/components/Timeline.jsx`
- จะเจอตัวแปร `const schedule = [ { time: "07:09", title: "...", ... } ]`
- สามารถแก้เวลา, ชื่อกิจกรรม, และรายละเอียดในวงเล็บปีกกานั้นได้เลย หากต้องการเพิ่มให้คัดลอกเพิ่มลงมา

### 4.4 ฟอร์มตอบรับการเข้าร่วมงาน (RSVP)
- ไปที่: `src/components/RSVPForm.jsx`
- สามารถแก้วันที่กำหนดให้ตอบรับ: หาคำว่า `กรุณาตอบรับการเข้าร่วมงานภายในวันที่...`
- หากต้องการเพิ่มตัวเลือก "จำนวนผู้ติดตาม" ให้คัดลอก `<option value="x">...ท่าน</option>` เพิ่มได้เลย

### 4.5 แผนที่สถานที่จัดงาน และ ที่พักแนะนำ
- ไปที่: `src/components/LocationMap.jsx`
- **สถานที่จัดงาน:** เลื่อนลงมาแก้ที่คำว่า "อรัญประเทศ" และเปลี่ยนลิงก์แผนที่ (href) ในปุ่ม `เปิดนำทางสถานที่จัดงาน`
- **ที่พักแนะนำ:** เลื่อนหาบรรทัดที่พัก 1-4 แล้วแก้ชื่อ รวมถึงลิงก์แผนที่ Google Maps ได้เลย

### 4.6 เลขบัญชี และข้อมูลโอนเงิน
- ไปที่: `src/components/GiftRegistry.jsx`
- เลขบัญชี: แก้ที่ `const accountNumber = "281-1-37864-8";`
- ชื่อธนาคารและชื่อบัญชี: เลื่อนลงมาแก้ที่ข้อความ "ธนาคารกรุงศรี (Krungsri)" และ "นาย วุฒิชัย เจิมเกาะ"

---

## 🚀 5. วิธีนำไฟล์ที่แก้เสร็จแล้ว ขึ้นเว็บจริง (Netlify)

ทุกครั้งที่มีการแก้โค้ด เปลี่ยนรูป หรือเปลี่ยนเพลง จะต้องสร้างไฟล์ที่พร้อมทำงานจริงใหม่เสมอ โดยทำตามขั้นตอนดังนี้:

1. เปิด **Terminal** ในเครื่อง Mac
2. พิมพ์คำสั่งเพื่อเข้าโฟลเดอร์ (หากยังไม่ได้เข้า):
   `cd /Users/warapornluanrit/Library/CloudStorage/OneDrive-ไลบรารีที่แชร์-onedrive/webcard`
3. พิมพ์คำสั่งสร้างเว็บไซต์:
   `npm run build`
4. รอจนขึ้นข้อความว่า `✓ built in ...ms`
5. ไปเปิดหน้าต่าง Finder เข้าโฟลเดอร์โปรเจกต์ `webcard`
6. จะพบโฟลเดอร์ที่ชื่อว่า **`dist`**
7. เปิดหน้าเว็บไซต์ **Netlify** เข้าไปที่เมนู **Deploys** ของเว็บไซต์คุณ
8. **ลากโฟลเดอร์ `dist`** จากเครื่องคุณ ไปวางตรงกรอบสำหรับอัปโหลด
9. รอระบบโหลดเสร็จ เว็บไซต์หลักจะได้รับการอัปเดตเป็นเวอร์ชันล่าสุดทันที!
